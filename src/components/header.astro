<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Florser | Inicio</title>

  <!-- Estilos personalizados -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet"> <!-- Font Awesome -->
  <style>
    /* Reset de margen y padding para evitar desbordamientos */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box; 
      font-family: "reboto",sans-serif;   
    }

    /* Estilos para el encabezado */
    header {
      display: flex;
      justify-content: space-between;  /* Alinea los elementos a la izquierda y derecha */
      align-items: center;
      padding: 10px 20px;
      background-color:whitesmoke; 
      width: 100%;
      height: 11vw; /* Ajusté la altura a 100px */
    }

    /* Sección de logo */
    .logo {
      display: flex;
      align-items: center;
    }

    .logo img {
      width: 50%;  /* Tamaño reducido del logo (ajusté al 50%) */
      height: auto; /* Mantiene la proporción de la imagen */
      margin-right: 70px;
      margin: auto 1vw;
      padding: 0;
    }

    h2.Florser {
      font-size: 1.5rem;
    }

    /* Sección de contacto (derecha) */
    .contact-info {
      display: flex;
      flex-direction: column;  /* Organiza los elementos en una columna */
      align-items: flex-end;   /* Alinea los elementos al final (derecha) */
      text-align: right;  /* Alinea el texto a la derecha */
    }

    .contact-info span {
      font-size: 1rem;
      color: black;
      margin-bottom: 5px;
      display: flex;
      align-items: center; /* Alinea el icono con el texto */
    }

    .contact-info i {
      margin-right: 8px; /* Espacio entre el icono y el texto */
      color: #333; /* Color del icono */
    }

    /* Estilos para la galería */
    .photos {
      margin-top: 50px;
      padding: 0 20px;
    }

    .gallery {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
      max-width: 60%;
      margin: 0 auto;
    }

    .img {
      position: relative;
      overflow: hidden;
      border-radius: 9px;
      background-color: #f0f0f0;
      height: 200px;
    }

    .img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .img:hover img {
      transform: scale(1.05);
    }

    /* Mapa de Google */
    .google-maps {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
      padding: 20px;
    }

    #google-maps {
      width: 100%;
      aspect-ratio: 16 / 9;
      max-width: 900px;
      height: auto;
      border: 0;
    }

    .map-title {
      position: absolute;
      top: 20px;
      font-size: 24px;
      color: black;
      z-index: 10;
      background-color: rgba(255, 255, 255, 0.7);
      padding: 10px;
      border-radius: 5px;
      font-weight: bold;
    }

    /* Responsividad */
    @media (max-width: 768px) {
      .gallery {
        grid-template-columns: repeat(2, 1fr);
      }

      #google-maps {
        max-width: 100%;
        height: 250px;
      }
    }

    @media (max-width: 480px) {
      .gallery {
        grid-template-columns: 1fr;
      }

      #google-maps {
        height: 200px;
      }
    }
  </style>
</head>

<body>
  <!-- Usamos el componente Header -->
  <header>
    <!-- Logo y título a la izquierda -->
    <div class="logo">
      <img src="/public/Logo-oficial.webp" alt="logo de terapia ocupacional" />
    </div>

    <!-- Información de contacto a la derecha -->
    <nav>
      <div class="contact-info">
        <span><i class="fab fa-instagram"></i>Instagram: Centroflorser</span>   
        <span><i class="fas fa-map-marker-alt"></i>Ubicación: Michimalongo 526</span>
      </div>
    </nav>
  </header>
</body>
</html>
